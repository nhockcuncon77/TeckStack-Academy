# Optional: reCAPTCHA v2 site key (get from https://www.google.com/recaptcha/admin)
# When set, the Apply form shows the "I'm not a robot" checkbox.
VITE_RECAPTCHA_SITE_KEY=your-recaptcha-site-key

# Production: full URL of your deployed API (e.g. https://your-api.railway.app).
# Required when frontend is on Vercel so login/admin/apply call the real API.
# Leave empty in local dev (Vite proxy handles /api).
VITE_API_URL=
